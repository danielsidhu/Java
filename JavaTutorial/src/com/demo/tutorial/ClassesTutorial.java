package com.demo.tutorial;

public class ClassesTutorial {

	//Classes are a blueprint for creating objects
	//Objects are an instance of a class
	
	/*
	 * Example is a Car Class
	 * has fields like started, currentGear and currentSpeed
	 * has methods like brake(), start(), changeGear()
	 * 
	 * Can create car objects like car1, car2, car3
	 */
	
	//refer to the textBox class as the example
	
	/*
	 * 2 places of memory management: the heap vs the stack
	 * the stack stores primitives and short lived variables
	 * the heap stores objects
	 */
}
